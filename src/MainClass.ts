export class MainClass {
  constructor() {}
  add(val: number): number {
    return val + 1;
  }
  sub(val: number): number {
    return val - 1;
  }
  foo(val: number): number {
    return val;
  }
}
